var React = require('react');

var FunForm = React.createClass({
  render: function () {
    return (
      <div id="toggleProfile">
        <form>
          <label> login</label>
        </form>
      </div>

    );
  }
});

module.exports = FunForm;
