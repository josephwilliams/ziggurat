var React = require('react');

var FunForm = React.createClass({
  render: function () {
    return (
      <div id="toggleProfile">
        auth forms
      </div>

    );
  }
});

module.exports = FunForm;
